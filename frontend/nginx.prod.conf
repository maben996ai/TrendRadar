server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # SPA 路由兜底：所有路径都返回 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 静态资源长缓存
    location /assets/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
